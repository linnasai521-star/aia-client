# Changelog

## [1.3.0] - 2026-05-14

### 新增
- **角色大厅（CharacterHall）**：全新卡片流界面，展示所有导入的角色
  - 毛玻璃角色卡片，含头像、名称、描述、在线状态
  - 底部虚线导入按钮，支持导入 .json / .png 角色卡
  - 空状态提示引导用户导入第一个角色
- **会话系统（Chats）**：每个角色支持多个独立聊天会话
  - 角色-会话双层架构，互不干扰
  - 自动为已有角色创建默认聊天会话
  - 聊天列表页：查看和管理角色的所有对话
  - 新建对话按钮：一键为新对话生成唯一会话
- **聊天列表页（ChatListPage）**：展示角色下所有会话
  - 会话标题、摘要预览、相对时间显示
  - 返回按钮返回角色大厅

### 改进
- **底部导航**："角色"标签现在进入角色大厅（替代旧的字符列表页）
- **导航流程**：角色大厅 → 会话列表 → 聊天页 完整链路
- **消息关联**：所有消息写入时自动关联 chatId
- **数据结构**：支持角色-会话双层架构，对话独立存储

### 技术
- 升级 IndexedDB schema v2 → v3
- 新增 `chats` 表（chatId 主键，characterId / updatedAt 索引）
- 扩展 `messages` 表支持 `chatId` 索引
- 新增 characters 表 `defaultChatId` 字段
- 向后兼容自动迁移：自动为已有角色创建默认chat并迁移消息

### 修复
- DB 迁移中 `getAll().result` 改为 `e.target.result` 异步正确访问
- `openChat` 正确关联 chatId 到对话和消息
- `createConv` 添加 characterId 字段
- 底部导航在 characterHall / chatList 页正确高亮"角色"

---

## [1.2.0] - 2026-05-13

### 新增
- **CSS 视觉系统全面升级**：明亮梦幻风格（粉紫渐变 + 毛玻璃）
  - Design Tokens 变量系统
  - 统一空间系统（space-1 ~ space-7）
  - 卡片呼吸感系统
  - 底部导航原生化（毛玻璃 + 选中指示器）
- **消息气泡美化**：AI 白色毛玻璃 + 用户粉紫渐变，滑入动画
- **角色卡美化**：毛玻璃卡片 + 顶部渐变装饰线
- **字体与动画统一**：禁止点击高亮、统一字体、微动效
- **聊天页头部优化**：毛玻璃头部 + 头像呼吸光 + 状态胶囊
- **角色大厅卡片**：64px 圆角头像、标签、预览文字
- **角色详情页打磨**：头像呼吸光、在线指示器、渐变名称
- **CharacterPage.js 卡片流重构**：列表式 → 毛玻璃卡片布局

### 改进
- 滚动条隐藏但保留滚动功能
- 快捷回复按钮毛玻璃增强
- 输入框浮动设计 + focus 发光
- 发送按钮渐变发光动画

---

## [1.1.0] - 2026-05-12

### 新增
- IndexedDB 本地数据库存储
- 多会话管理（conversations CRUD）
- 角色卡导入（PNG/JSON 格式）
- 世界书/Lorebook 支持
- API 配置管理（多配置切换）
- 消息流式/非流式回复
- PIN 锁屏保护
- 暗色/沉浸式 UI 基础样式

### 技术
- React (createElement) 无编译构建
- IndexedDB v2 schema
- GitHub Pages 部署
